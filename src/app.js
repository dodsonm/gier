const css = require('./app.scss');

import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <h1>Now using Google's Material UI library!</h1>
  <p>Welcome to the wonderful world of React.js</p>,
  document.getElementById('root')
);
